export interface Itheme {
  colors: {
    grey: string;
    green: string;
    blue: string;
    red: string;

    primaryText: string;
  };
}

export const theme: Itheme = {
  colors: {
    grey: '#808080',
    green: '#00ff00',
    blue: '#0089ff',
    red: '#ff0000',

    primaryText: '#808080',
  },
};
